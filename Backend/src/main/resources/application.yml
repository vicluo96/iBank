spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bank?characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: your_username
    password: your_password
server:
  port: 8080
  servlet:
    context-path: /bank
mybatis:
  mapper-locations: com/group3/brokebank/mapper/*.xml
logging:
  level:
    com:
      group3:
        brokebank:
          mapper: debug
